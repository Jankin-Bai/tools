{
  "Name": "gitsync",
  "DisplayName": "Git Sync",
  "Icon": "shell32.dll,14",
  "CommandTemplate": "powershell.exe -NoProfile -ExecutionPolicy Bypass -File \"{ToolDir}\\Git-Sync.ps1\" -Path \"{PathParam}\"",
  "Script": "Git-Sync.ps1",
  "Contexts": ["Folder", "FolderBackground", "Desktop"],
  "AppliesTo": "Directory",
  "Description": "Run git add -A && git commit -m 'sync at <timestamp>' && git push origin <branch> in the selected folder",
  "Mcp": {
    "Enabled": true,
    "Description": "Stage all changes, commit with timestamp message, and push to origin. Side effects: creates a commit and pushes to remote. Requires a git repository with an 'origin' remote.",
    "InputSchema": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "Absolute path to a folder containing a git repository"
        }
      },
      "required": ["path"]
    }
  }
}
